/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images-na.ssl-images-amazon.com', 'www.uline.ca'],
  },
  output: 'standalone',
}

module.exports = nextConfig
